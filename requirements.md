# 客服管理系统需求文档

## 1. 系统概述

本系统是一个客服管理后台，主要用于管理和查看用户与AI客服的对话记录，支持人工接入和对话详情查看功能。

## 2. 页面功能需求

### 2.1 历史对话记录页面

#### 2.1.1 页面操作

**搜索功能**
- 用户可以通过搜索框输入关键词
- 支持按对话标题搜索
- 支持按用户名搜索
- 实时搜索，输入即时过滤结果

**时间范围筛选**
- 支持选择开始日期和结束日期
- 日期选择器格式：YYYY-MM-DD
- 可以只选择开始日期或结束日期
- 结束日期包含当天的全部时间（23:59:59）

**人工接入状态筛选**
- 全部状态：显示所有对话记录
- 已人工接入：只显示已转人工的对话
- 未人工接入：只显示纯AI对话的记录

**清除筛选**
- 当存在任何筛选条件时显示"清除筛选"按钮
- 点击后清空所有筛选条件，恢复显示全部记录

**查看详情**
- 每行记录提供"查看详情"按钮
- 点击后跳转到对话详情页面

**列表展示**
- 表格形式展示对话记录
- 支持鼠标悬停高亮行
- 长标题自动截断并显示完整标题的提示

#### 2.1.2 字段含义

| 字段名 | 字段含义 | 数据类型 | 显示格式 |
|--------|----------|----------|----------|
| 对话标题 | 对话的主题或用户首次提问的内容摘要 | 字符串 | 文本，超长截断 |
| 对话人名称 | 发起对话的用户名或用户ID | 字符串 | 文本 + 头像图标 |
| 时间 | 对话开始的时间 | 日期时间 | YYYY-MM-DD HH:mm:ss |
| 召唤次数 | 用户在该对话中请求人工客服的次数 | 数字 | 整数 |
| 人工接入状态 | 该对话是否有人工客服介入 | 布尔值 | 已接入(红色徽章) / 未接入(绿色徽章) |
| 操作 | 可执行的操作按钮 | - | 查看详情按钮 |

### 2.2 对话详情页面

#### 2.2.1 页面操作

**返回功能**
- 提供返回按钮，返回历史对话记录列表
- 显示当前查看的对话标题

**消息展示**
- 按时间顺序展示对话消息
- 区分用户消息和AI回复
- 支持消息的点赞/点踩操作

**人工接入操作**
- 显示人工接入状态
- 提供接入人工客服的操作按钮（如果尚未接入）

**消息互动**
- 对AI回复支持点踩操作
- 显示消息发送时间
- 显示消息发送者信息

#### 2.2.2 字段含义

**对话基本信息**
| 字段名 | 字段含义 | 数据类型 | 显示格式 |
|--------|----------|----------|----------|
| 对话标题 | 当前查看的对话主题 | 字符串 | 页面标题 |
| 用户名 | 对话参与的用户名 | 字符串 | 文本 |
| 对话时间 | 对话发生的时间范围 | 日期时间 | 开始时间-结束时间 |

**消息详情**
| 字段名 | 字段含义 | 数据类型 | 显示格式 |
|--------|----------|----------|----------|
| 消息ID | 消息的唯一标识 | 数字 | 隐藏字段，用于系统识别 |
| 消息类型 | 区分用户消息还是AI回复 | 枚举 | user(用户) / ai(AI助手) |
| 消息内容 | 消息的具体文本内容 | 字符串 | 文本，支持换行 |
| 发送时间 | 消息发送的具体时间 | 日期时间 | YYYY-MM-DD HH:mm:ss |
| 发送者 | 消息发送者的用户名 | 字符串 | 仅用户消息显示 |
| 详细信息 | AI回复的补充说明或建议列表 | 字符串数组 | 列表形式展示 |
| 点踩状态 | 用户对AI回复的反馈 | 布尔值 | 点踩图标的激活状态 |

## 3. 数据结构说明

### 3.1 对话记录数据结构
\`\`\`typescript
interface ConversationRecord {
  id: string;                    // 对话唯一标识
  title: string;                 // 对话标题
  userName: string;              // 用户名
  time: string;                  // 对话时间
  isHandedOver: boolean;         // 是否人工接入
  summonCount: number;           // 召唤次数
  messages: Message[];           // 消息列表
}
\`\`\`

### 3.2 消息数据结构
\`\`\`typescript
interface Message {
  id: number;                    // 消息ID
  type: 'user' | 'ai';          // 消息类型
  content: string;               // 消息内容
  time: string;                  // 发送时间
  user?: string;                 // 发送者（用户消息）
  details?: string[];            // 详细信息（AI消息）
  isDisliked: boolean;           // 是否被点踩
}
\`\`\`

## 4. 用户交互流程

### 4.1 查看历史对话流程
1. 用户进入历史对话记录页面
2. 系统展示所有对话记录列表
3. 用户可选择使用搜索或筛选功能
4. 系统实时更新显示结果
5. 用户点击"查看详情"按钮
6. 系统跳转到对应的对话详情页面

### 4.2 对话详情查看流程
1. 用户从历史记录页面进入详情页
2. 系统展示完整的对话内容
3. 按时间顺序显示所有消息
4. 用户可以查看消息详情和互动记录
5. 用户可以返回历史记录列表

## 5. 界面设计要求

### 5.1 响应式设计
- 支持桌面端和移动端访问
- 表格在小屏幕上保持可读性
- 筛选器在移动端自适应布局

### 5.2 用户体验
- 加载状态提示
- 空数据状态展示
- 操作反馈提示
- 错误状态处理

### 5.3 视觉设计
- 统一的色彩方案
- 清晰的信息层级
- 合适的间距和字体大小
- 直观的图标和按钮设计

## 6. 技术实现要求

### 6.1 前端技术栈
- React + TypeScript
- Next.js 框架
- Tailwind CSS 样式
- shadcn/ui 组件库

### 6.2 数据管理
- 本地状态管理（useState）
- 数据筛选和搜索逻辑
- 组件间数据传递

### 6.3 性能要求
- 列表数据虚拟化（大数据量时）
- 搜索防抖处理
- 组件懒加载
